<template>
  <div class="container">
    <v-row>
      <v-col cols="12">
        <v-card color="primary" flat shaped id="policy">
          <v-card-title>
            <h4>{{ $t("policy.title") }}</h4>
          </v-card-title>
          <v-card-text>
            <p class="white--text">{{ $t("policy.data") }}</p>
          </v-card-text>
        </v-card>
      </v-col>
    </v-row>
  </div>
</template>